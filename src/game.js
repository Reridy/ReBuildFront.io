import {WORLD,PLAYER,RESOURCES,ZONES,NODE_TYPES,BUILDINGS,ENEMIES,AI_PLAYERS,RESEARCH} from './config.js';
import {ITEM_DEFS,RECIPES,starterInventory,addItem,canFitItem,stackLimit,itemName} from './data.js';
import {createI18n} from './i18n.js';
const root=document.getElementById('gameRoot'),i18n=createI18n();
const clamp=(v,a,b)=>Math.max(a,Math.min(b,v)),dist=(a,b)=>Math.hypot(a.x-b.x,a.y-b.y),rand=(a,b)=>a+Math.random()*(b-a),norm=(x,y)=>{const m=Math.hypot(x,y)||1;return{x:x/m,y:y/m}},grid=()=>WORLD.gridSize,esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const KO={wood:'나무',stone:'돌',copper:'구리',iron:'철',crystal:'크리스탈',Sword:' 검',Pickaxe:' 곡괭이',Axe:' 도끼',hammer:'망치',workbench:'제작대',researchBench:'연구 작업대',wallItem:'벽',generatorItem:'발전기',relayItem:'수리 중계기',depotItem:'보급고',plank:'판자',cord:'끈',metalPlate:'금속판',circuit:'회로',armorShell:'강화 외피'};
const TXT={en:{inventory:'Inventory',craft:'Crafting',recipes:'Recipes',search:'Search recipes',research:'Research',build:'Build',cancel:'Cancel',left:'Left click: pick/place all',right:'Right click: split/place one',shift:'Shift-click: quick move',double:'Double-click: collect matching stacks',outside:'Click outside inventory to drop held items',interact:'E · use nearby station / inventory',ready:'READY',pause:'Pause',resume:'Resume',return:'Return to lobby',objective:'OBJECTIVE',prep:'Explore, gather and prepare.',warning:'Return to HEART. Invasion incoming.',wave:'Defend HEART.',recovery:'Repair and expand.',overtime:'OVERTIME',remaining:'Enemies',holding:'Held',stationTooFar:'Move closer to the station.',needHammer:'Hold the hammer to build.',noStructure:'Craft a structure first.',blocked:'This space is blocked.',locked:'Research required.',crafted:'Crafted',noMaterials:'Not enough materials.',researchDone:'Research complete.',researchBench:'Place and approach a Research Bench.',workbench:'Place and approach a Workbench.',toolHint:'Sword = fast combat · Pickaxe = minerals/stone · Axe = wood/organic resources',spawnClear:'Resources will not respawn near structures.'},ko:{inventory:'인벤토리',craft:'제작',recipes:'제작법',search:'제작법 검색',research:'연구',build:'건설',cancel:'취소',left:'좌클릭: 스택 전체 들기 / 전체 놓기',right:'우클릭: 절반 들기 / 1개 놓기',shift:'Shift+클릭: 핫바 ↔ 인벤토리 빠른 이동',double:'더블클릭: 같은 아이템 한 스택까지 모으기',outside:'인벤토리 밖 클릭: 들고 있는 아이템 버리기',interact:'E · 근처 작업대 사용 / 인벤토리',ready:'준비 완료',pause:'일시정지',resume:'계속하기',return:'로비로 돌아가기',objective:'목표',prep:'탐험하고 자원을 모아 준비하세요.',warning:'HEART로 복귀하세요. 침공이 임박했습니다.',wave:'HEART를 방어하세요.',recovery:'수리하고 방어선을 확장하세요.',overtime:'연장전',remaining:'남은 적',holding:'커서',stationTooFar:'작업대에 더 가까이 가세요.',needHammer:'건설하려면 망치를 들어야 합니다.',noStructure:'먼저 제작대에서 건물을 제작하세요.',blocked:'이 위치에는 설치할 수 없습니다.',locked:'선행 연구가 필요합니다.',crafted:'제작 완료',noMaterials:'재료가 부족합니다.',researchDone:'연구 완료',researchBench:'연구 작업대를 설치한 뒤 가까이 가세요.',workbench:'제작대를 설치한 뒤 가까이 가세요.',toolHint:'검 = 빠른 전투 · 곡괭이 = 광물/돌 채집 · 도끼 = 나무/유기 자원 채집',spawnClear:'건물 주변에는 자원이 다시 생성되지 않습니다.'},ja:{inventory:'インベントリ',craft:'クラフト',recipes:'レシピ',search:'レシピ検索',research:'研究',build:'建築',cancel:'キャンセル',left:'左クリック: 全部持つ/置く',right:'右クリック: 半分持つ/1個置く',shift:'Shiftクリック: クイック移動',double:'ダブルクリック: 同じアイテムを集める',outside:'外側クリック: 手持ちを捨てる',interact:'E · 作業台/インベントリ',ready:'準備完了',pause:'一時停止',resume:'続ける',return:'ロビーへ戻る',objective:'目標',prep:'探索して準備しよう。',warning:'HEARTへ戻ろう。',wave:'HEARTを守ろう。',recovery:'修理して拡張しよう。',overtime:'延長戦',remaining:'残り',holding:'カーソル',stationTooFar:'作業台に近づいてください。',needHammer:'ハンマーを持って建築します。',noStructure:'先に建物をクラフトしてください。',blocked:'ここには置けません。',locked:'研究が必要です。',crafted:'作成完了',noMaterials:'材料不足',researchDone:'研究完了',researchBench:'研究台を設置して近づいてください。',workbench:'作業台を設置して近づいてください。',toolHint:'剣=戦闘 · つるはし=鉱物 · 斧=木材',spawnClear:'建物の近くには資源が再出現しません。'},zh:{inventory:'物品栏',craft:'制作',recipes:'配方',search:'搜索配方',research:'研究',build:'建造',cancel:'取消',left:'左键：拿起/放下全部',right:'右键：拿一半/放1个',shift:'Shift点击：快速移动',double:'双击：收集同类物品',outside:'点击物品栏外：丢弃手持物品',interact:'E · 使用工作台/物品栏',ready:'准备完成',pause:'暂停',resume:'继续',return:'返回大厅',objective:'目标',prep:'探索并准备。',warning:'返回HEART。',wave:'守护HEART。',recovery:'修理并扩建。',overtime:'加时',remaining:'剩余敌人',holding:'光标',stationTooFar:'请靠近工作台。',needHammer:'手持锤子才能建造。',noStructure:'请先制作建筑物品。',blocked:'这里无法放置。',locked:'需要研究。',crafted:'制作完成',noMaterials:'材料不足。',researchDone:'研究完成。',researchBench:'放置研究台后靠近使用。',workbench:'放置工作台后靠近使用。',toolHint:'剑=战斗 · 镐=矿石/石头 · 斧=木材',spawnClear:'建筑附近不会刷新资源。'}};
const tt=k=>TXT[i18n.lang]?.[k]??TXT.en[k]??k;
function label(id){if(i18n.lang==='ko'){if(KO[id])return KO[id];const m=id.match(/^(wood|stone|copper|iron|crystal)(Sword|Pickaxe|Axe)$/);if(m)return `${KO[m[1]]}${KO[m[2]]}`}return itemName(id)}
let S=null,canvas=null,ctx=null,U={},bound=false;
window.addEventListener('rbf-start-game',e=>start(e.detail));
function makePlayer(name,ai=false,color='#e9f4ff'){return{name,ai,color,x:WORLD.heart.x+rand(-55,55),y:WORLD.heart.y+105+rand(-30,30),radius:ai?12:PLAYER.radius,maxHp:ai?115:PLAYER.maxHp,hp:ai?115:PLAYER.maxHp,dead:false,cd:0,dashCd:0,dashT:0,gatherCd:0,inventory:starterInventory(),bubble:null,think:rand(.1,.35),goal:null}}
function start(detail){i18n.set(detail.language||i18n.lang);root.innerHTML=markup();root.classList.remove('hidden');canvas=root.querySelector('#gameCanvas');ctx=canvas.getContext('2d');U=Object.fromEntries([...root.querySelectorAll('[id]')].map(e=>[e.id,e]));const aiCount=Math.max(0,Math.min(AI_PLAYERS.length,Number(detail.room?.aiCount)||0));S={detail,last:performance.now(),time:0,keys:new Set(),mouse:{x:640,y:360},camera:{x:0,y:0},phase:'prep',phaseTime:WORLD.prepSeconds,wave:0,score:0,bank:Object.fromEntries(Object.keys(RESOURCES).map(k=>[k,0])),tech:new Set(),heart:{...WORLD.heart,hp:WORLD.heart.maxHp,maxHp:WORLD.heart.maxHp},player:makePlayer(detail.playerName||'User1'),ais:AI_PLAYERS.slice(0,aiCount).map(a=>makePlayer(a.name,true,a.accent)),buildings:[],nodes:[],enemies:[],drops:[],particles:[],selectedHotbar:0,buildType:null,modal:null,cursor:null,armor:null,paused:false,over:false,waypoint:null,recipeQuery:'',cache:{}};Object.assign(S.bank,{wood:18,stone:12,crystal:4});seedWorld();bindGlobal();bindInstance();renderHotbar();renderHUD(true);toast(tt('toolHint'));requestAnimationFrame(loop)}
function markup(){return `<main class="game-shell"><div class="game-top"><div class="hud-strip"><div class="hud-card"><span>HEART</span><strong id="heartHp"></strong><div class="meter"><i id="heartBar"></i></div></div><div class="hud-card"><span>WAVE</span><strong id="waveText">0</strong><small id="phaseText"></small></div><div class="hud-card"><span>ZONE</span><strong id="zoneText"></strong></div><div class="hud-card"><span>DASH</span><strong id="dashText">${tt('ready')}</strong><div class="meter"><i id="dashBar"></i></div></div><div class="hud-card"><span>SCORE</span><strong id="scoreText">0</strong></div></div><div class="row"><button id="helpBtn" class="btn">?</button><button id="pauseBtn" class="btn">${tt('pause')}</button></div></div><section class="game-layout"><aside class="side-panel compact-panel"><h3>${tt('holding')}</h3><div id="heldItem" class="selected-tool"></div><p class="muted">${tt('toolHint')}</p><p class="muted">${tt('interact')}</p><h3>TEAM STORAGE</h3><div id="resourceGrid" class="resource-grid"></div></aside><section class="arena-wrap"><canvas id="gameCanvas" width="1280" height="720"></canvas><div id="objective" class="objective-card"></div><div id="buildDock" class="build-dock hidden"></div><div id="interactionHint" class="interaction-hint"></div><div id="banner" class="banner hidden"></div><div id="toast" class="toast hidden"></div><div id="hotbar" class="hotbar"></div><div id="gameModal" class="inventory-modal hidden"></div><div id="cursorStack" class="cursor-stack hidden"></div><div id="overlay" class="overlay-panel hidden"></div></section><aside class="side-panel"><h3>SQUAD</h3><div id="squadList"></div><h3>${tt('inventory')}</h3><button id="inventoryButton" class="btn full">E · ${tt('inventory')}</button><div id="itemInspect" class="selection-info"></div><p class="muted">${tt('spawnClear')}</p></aside></section><div class="chat-box"><input id="chatInput" class="chat-input" maxlength="180" placeholder="CHAT"></div></main>`}
const all=()=>[S.player,...S.ais],live=()=>all().filter(p=>!p.dead),held=()=>S.player.inventory[S.selectedHotbar],heldDef=()=>ITEM_DEFS[held()?.id],zoneAt=(x,y)=>ZONES.find(z=>x>=z.x&&x<z.x+z.w&&y>=z.y&&y<z.y+z.h)||ZONES[4];
function seedWorld(){S.nodes=[];for(const z of ZONES)for(let i=0;i<(z.id==='heartlands'?20:15);i++){const p=safePoint(z),type=z.nodeTypes[Math.floor(Math.random()*z.nodeTypes.length)],d=NODE_TYPES[type];S.nodes.push({type,zone:z.id,x:p.x,y:p.y,radius:d.radius,hp:d.hp,maxHp:d.hp,color:d.color,respawn:0})}}
function safePoint(z){for(let tries=0;tries<35;tries++){const p={x:rand(z.x+45,z.x+z.w-45),y:rand(z.y+45,z.y+z.h-45)};if(dist(p,S.heart)<130)continue;if(S.buildings.every(b=>dist(p,b)>WORLD.nodeSpawnClearance))return p}return{x:z.x+70,y:z.y+70}}
function respawnNode(n){const z=ZONES.find(z=>z.id===n.zone),p=safePoint(z),type=z.nodeTypes[Math.floor(Math.random()*z.nodeTypes.length)],d=NODE_TYPES[type];Object.assign(n,{type,x:p.x,y:p.y,radius:d.radius,hp:d.hp,maxHp:d.hp,color:d.color,respawn:0})}
function nearest(arr,p,range,filter=()=>true){let best=null,bd=range;for(const o of arr)if(filter(o)){const d=dist(p,o);if(d<bd){best=o;bd=d}}return best}
function drop(id,count,x,y){S.drops.push({id,count,x:x+rand(-10,10),y:y+rand(-10,10),radius:7})}
function pickup(p){for(let i=S.drops.length-1;i>=0;i--){const d=S.drops[i];if(dist(p,d)<p.radius+d.radius+10){const left=addItem(p.inventory,d.id,d.count,S.tech.has('logistics')?16:0);if(!left)S.drops.splice(i,1);else d.count=left;if(p===S.player)renderHotbar()}}}
function harvest(n){const d=NODE_TYPES[n.type];drop(d.resource,d.amount,n.x,n.y);if(d.secondary&&Math.random()<d.secondaryChance)drop(d.secondary,Math.max(1,Math.floor(d.amount*.35)),n.x,n.y);n.hp=0;n.respawn=S.time+10+Math.random()*7;S.score+=2}
function toolPower(node,def){if(!def?.toolType)return{damage:4,cooldown:.8};const match=def.toolType===NODE_TYPES[node.type].tool;return{damage:(match?28:6)*(def.gather||1),cooldown:Math.max(.22,def.cooldown||.6)}}
function attackOrGather(){if(S.player.dead||S.paused||S.modal||S.over||S.player.cd>0)return;const def=heldDef(),n=nearest(S.nodes,S.player,PLAYER.actionRange,x=>x.hp>0),e=nearest(S.enemies,S.player,PLAYER.actionRange,x=>x.hp>0&&x.spawnDelay<=0);if(n&&def?.toolType!=='sword'&&def?.toolType!=='hammer'){const p=toolPower(n,def);n.hp-=p.damage;S.player.cd=p.cooldown;if(n.hp<=0)harvest(n);return}if(e){const damage=def?.damage||6;e.hp-=damage;S.player.cd=def?.cooldown||.72;burst(e.x,e.y,'#d9efff',5);return}if(n){n.hp-=4;S.player.cd=.85;if(n.hp<=0)harvest(n)}}
function rect(b){return{x:b.gx*grid(),y:b.gy*grid(),w:b.gw*grid(),h:b.gh*grid()}}
function circleRect(x,y,r,R){const nx=clamp(x,R.x,R.x+R.w),ny=clamp(y,R.y,R.y+R.h);return Math.hypot(x-nx,y-ny)<r}
function blocked(e,x,y){for(const b of S.buildings)if(b.hp>0&&circleRect(x,y,e.radius,rect(b)))return true;return false}
function move(e,dx,dy,speed,dt){const n=norm(dx,dy),step=speed*dt;let nx=clamp(e.x+n.x*step,e.radius,WORLD.width-e.radius);if(!blocked(e,nx,e.y))e.x=nx;let ny=clamp(e.y+n.y*step,e.radius,WORLD.height-e.radius);if(!blocked(e,e.x,ny))e.y=ny}
function footprint(type,x,y){const b=BUILDINGS[type];return{gx:Math.floor(x/grid()),gy:Math.floor(y/grid()),gw:b.gridW,gh:b.gridH}}
function cells(f){const a=[];for(let y=0;y<f.gh;y++)for(let x=0;x<f.gw;x++)a.push(`${f.gx+x},${f.gy+y}`);return a}
function placementCheck(f){const R={x:f.gx*grid(),y:f.gy*grid(),w:f.gw*grid(),h:f.gh*grid()},occ=new Set(S.buildings.flatMap(cells));if(f.gx<0||f.gy<0||R.x+R.w>WORLD.width||R.y+R.h>WORLD.height)return false;if(cells(f).some(c=>occ.has(c)))return false;if(circleRect(S.heart.x,S.heart.y,S.heart.radius+42,R))return false;for(const p of[...live(),...S.enemies])if(p.hp>0&&circleRect(p.x,p.y,p.radius+2,R))return false;return true}
function findStructureItem(type){const id=BUILDINGS[type]?.item;return S.player.inventory.findIndex(s=>s?.id===id)}
function consumeSlot(i,n=1){const s=S.player.inventory[i];if(!s)return false;s.count-=n;if(s.count<=0)S.player.inventory[i]=null;return true}
function place(type,x,y){const spec=BUILDINGS[type];if(!spec)return;const def=heldDef();if(def?.toolType!=='hammer')return toast(tt('needHammer'));if(spec.research&&!S.tech.has(spec.research))return toast(tt('locked'));const idx=findStructureItem(type);if(idx<0)return toast(tt('noStructure'));const f=footprint(type,x,y);if(!placementCheck(f))return toast(tt('blocked'));consumeSlot(idx);const mul=type==='wall'?1+(S.tech.has('wallI')?.35:0)+(S.tech.has('wallII')?.35:0):1,hp=spec.hp*mul,b={type,...f,x:(f.gx+f.gw/2)*grid(),y:(f.gy+f.gh/2)*grid(),hp,maxHp:hp,color:spec.color,cd:0};S.buildings.push(b);for(const n of S.nodes)if(n.hp>0&&dist(n,b)<WORLD.nodeSpawnClearance){n.hp=0;n.respawn=S.time+5+Math.random()*4}burst(b.x,b.y,spec.color,12);renderHotbar();renderBuildDock()}
function buildingNear(type,range=130){return nearest(S.buildings,S.player,range,b=>b.type===type&&b.hp>0)}
function interaction(){const r=buildingNear('researchBench'),w=buildingNear('workbench');if(r)return{type:'research',building:r};if(w)return{type:'craft',building:w};return null}
function openContext(){if(S.modal)return closeModal();const hit=interaction();if(hit?.type==='research')return openResearch();if(hit?.type==='craft')return openCraft();openInventory()}
function slotHtml(s,i,cls=''){const d=s&&ITEM_DEFS[s.id];return `<button class="slot item-slot ${cls}" data-slot="${i}" title="${s?esc(label(s.id)):''}">${s?`<b class="item-icon">${d?.icon||'?'}</b><span class="item-short">${esc(label(s.id)).slice(0,8)}</span><em>${s.count>1?s.count:''}</em>`:''}<small>${i<9?i+1:''}</small></button>`}
function renderHotbar(){if(!S)return;U.hotbar.innerHTML=S.player.inventory.slice(0,9).map((s,i)=>slotHtml(s,i,S.selectedHotbar===i?'selected':'')).join('');U.hotbar.querySelectorAll('[data-slot]').forEach(b=>b.onclick=()=>selectHotbar(Number(b.dataset.slot)));renderHeld();renderBuildDock()}
function selectHotbar(i){S.selectedHotbar=i;const d=heldDef();if(d?.toolType!=='hammer')S.buildType=null;renderHotbar()}
function renderHeld(){const s=held(),d=heldDef();if(!s){U.heldItem.innerHTML='<span class="muted">—</span>';U.itemInspect.innerHTML='<span class="muted">—</span>';return}const stat=d?.toolType?`<small>${d.toolType.toUpperCase()} · DMG ${d.damage??'-'} · CD ${d.cooldown??'-'}s · EFF ${d.gather??1}×</small>`:`<small>${d?.kind||''}</small>`;U.heldItem.innerHTML=`<b>${d?.icon||'?'} ${esc(label(s.id))}</b>${stat}`;U.itemInspect.innerHTML=`<b>${esc(label(s.id))}</b>${stat}<p>${tt('left')}<br>${tt('right')}</p>`}
function renderBuildDock(){if(!S||!U.buildDock)return;const d=heldDef();if(d?.toolType!=='hammer'){S.buildType=null;U.buildDock.classList.add('hidden');return}const choices=Object.entries(BUILDINGS).filter(([type,spec])=>findStructureItem(type)>=0&&(!spec.research||S.tech.has(spec.research)));U.buildDock.classList.remove('hidden');U.buildDock.innerHTML=`<span>${tt('build')}</span>${choices.map(([type,spec])=>{const s=S.player.inventory[findStructureItem(type)];return`<button class="btn ${S.buildType===type?'primary':''}" data-build="${type}">${ITEM_DEFS[spec.item]?.icon||'▧'} ${esc(label(spec.item))} ×${s?.count||0}</button>`}).join('')||`<small>${tt('noStructure')}</small>`}<button class="btn danger" data-build-cancel>× ${tt('cancel')}</button>`;U.buildDock.querySelectorAll('[data-build]').forEach(b=>b.onclick=e=>{e.stopPropagation();S.buildType=b.dataset.build;renderBuildDock()});U.buildDock.querySelector('[data-build-cancel]').onclick=e=>{e.stopPropagation();S.buildType=null;renderBuildDock()}}
function openInventory(){S.modal='inventory';renderInventoryModal()}
function openCraft(){if(!buildingNear('workbench'))return toast(tt('stationTooFar'));S.modal='craft';renderCraftModal()}
function openResearch(){if(!buildingNear('researchBench'))return toast(tt('stationTooFar'));S.modal='research';renderResearchModal()}
function closeModal(){if(S.cursor)dropCursor(false);S.modal=null;U.gameModal.classList.add('hidden');U.gameModal.innerHTML='';renderCursor()}
function modalShell(title,body){U.gameModal.classList.remove('hidden');U.gameModal.innerHTML=`<div class="inventory-card advanced-inventory"><div class="inventory-title"><h2>${title}</h2><button class="btn" data-close>×</button></div>${body}</div>`;U.gameModal.querySelector('[data-close]').onclick=closeModal;U.gameModal.oncontextmenu=e=>e.preventDefault()}
function renderInventoryModal(){modalShell(tt('inventory'),`<div class="inventory-help"><span>${tt('left')}</span><span>${tt('right')}</span><span>${tt('shift')}</span><span>${tt('double')}</span><span>${tt('outside')}</span></div><div class="armor-slot"><b>ARMOR</b><button class="slot item-slot" id="armorSlot">${S.armor?`${ITEM_DEFS[S.armor.id]?.icon} ${esc(label(S.armor.id))}`:'—'}</button></div><div class="inventory-grid">${S.player.inventory.map((s,i)=>slotHtml(s,i)).join('')}</div>`);bindInventorySlots();U.gameModal.querySelector('#armorSlot').onclick=()=>{if(S.armor&&!S.cursor){S.cursor=S.armor;S.armor=null;renderInventoryModal();renderCursor()}}}
function bindInventorySlots(){U.gameModal.querySelectorAll('[data-slot]').forEach(el=>{const i=Number(el.dataset.slot);el.onmousedown=e=>{e.preventDefault();slotClick(i,e.button,e.shiftKey)};el.ondblclick=e=>{e.preventDefault();collectMatching(i)}});U.gameModal.onmousedown=e=>{if(e.target.closest('.item-slot,.recipe-card,.btn,input'))return;if(S.cursor){dropCursor(e.button===2);renderCurrentModal()}}}
function slotClick(i,button,shift){if(shift&&button===0)return quickMove(i);const slot=S.player.inventory[i];if(button===0){if(!S.cursor&&slot){S.cursor={...slot};S.player.inventory[i]=null}else if(S.cursor&&!slot){S.player.inventory[i]=S.cursor;S.cursor=null}else if(S.cursor&&slot?.id===S.cursor.id){const lim=stackLimit(slot.id,S.tech.has('logistics')?16:0),move=Math.min(lim-slot.count,S.cursor.count);slot.count+=move;S.cursor.count-=move;if(S.cursor.count<=0)S.cursor=null}else if(S.cursor&&slot){const tmp={...slot};S.player.inventory[i]=S.cursor;S.cursor=tmp}}else if(button===2){if(!S.cursor&&slot){const take=Math.ceil(slot.count/2);S.cursor={id:slot.id,count:take};slot.count-=take;if(slot.count<=0)S.player.inventory[i]=null}else if(S.cursor&&!slot){S.player.inventory[i]={id:S.cursor.id,count:1};if(--S.cursor.count<=0)S.cursor=null}else if(S.cursor&&slot?.id===S.cursor.id&&slot.count<stackLimit(slot.id,S.tech.has('logistics')?16:0)){slot.count++;if(--S.cursor.count<=0)S.cursor=null}}renderCurrentModal();renderCursor();renderHotbar()}
function quickMove(i){const s=S.player.inventory[i];if(!s)return;const fromHot=i<9,start=fromHot?9:0,end=fromHot?36:9;let left=s.count;for(let j=start;j<end&&left;j++){const d=S.player.inventory[j];if(d?.id===s.id&&d.count<stackLimit(s.id)){const n=Math.min(stackLimit(s.id)-d.count,left);d.count+=n;left-=n}}for(let j=start;j<end&&left;j++)if(!S.player.inventory[j]){S.player.inventory[j]={id:s.id,count:left};left=0}if(left)S.player.inventory[i].count=left;else S.player.inventory[i]=null;renderCurrentModal();renderHotbar()}
function collectMatching(i){const source=S.player.inventory[i];if(!S.cursor&&source){S.cursor={...source};S.player.inventory[i]=null}if(!S.cursor)return;const lim=stackLimit(S.cursor.id,S.tech.has('logistics')?16:0);for(let j=0;j<S.player.inventory.length&&S.cursor.count<lim;j++){const s=S.player.inventory[j];if(s?.id===S.cursor.id){const n=Math.min(s.count,lim-S.cursor.count);S.cursor.count+=n;s.count-=n;if(s.count<=0)S.player.inventory[j]=null}}renderCurrentModal();renderCursor();renderHotbar()}
function dropCursor(one){if(!S.cursor)return;const n=one?1:S.cursor.count;drop(S.cursor.id,n,S.player.x,S.player.y);S.cursor.count-=n;if(S.cursor.count<=0)S.cursor=null}
function renderCursor(){if(!S||!U.cursorStack)return;if(!S.cursor){U.cursorStack.classList.add('hidden');return}const d=ITEM_DEFS[S.cursor.id];U.cursorStack.classList.remove('hidden');U.cursorStack.innerHTML=`<b>${d?.icon||'?'} ${esc(label(S.cursor.id))}</b><span>${S.cursor.count}</span>`}
window.addEventListener('mousemove',e=>{if(U.cursorStack){U.cursorStack.style.left=`${e.clientX+14}px`;U.cursorStack.style.top=`${e.clientY+14}px`}});
function countAvailable(id){let n=S.bank[id]||0;for(const s of S.player.inventory)if(s?.id===id)n+=s.count;return n}
function canRecipe(r){return Object.entries(r.ingredients).every(([id,n])=>countAvailable(id)>=n)}
function consumeIngredient(id,n){for(let i=0;i<S.player.inventory.length&&n;i++){const s=S.player.inventory[i];if(s?.id===id){const take=Math.min(n,s.count);s.count-=take;n-=take;if(s.count<=0)S.player.inventory[i]=null}}if(n){const take=Math.min(n,S.bank[id]||0);S.bank[id]-=take;n-=take}return n===0}
function recipeLocked(r){const structure=ITEM_DEFS[r.out.id]?.structure,spec=structure&&BUILDINGS[structure];return !!(spec?.research&&!S.tech.has(spec.research))}
function craftRecipe(id,many=false){const r=RECIPES.find(r=>r.id===id);if(!r||recipeLocked(r))return toast(tt('locked'));let made=0;do{if(!canRecipe(r)||!canFitItem(S.player.inventory,r.out.id,r.out.count,S.tech.has('logistics')?16:0))break;for(const[k,v]of Object.entries(r.ingredients))consumeIngredient(k,v);addItem(S.player.inventory,r.out.id,r.out.count,S.tech.has('logistics')?16:0);made+=r.out.count}while(many&&made<64);if(!made)return toast(tt('noMaterials'));toast(`${tt('crafted')}: ${label(r.out.id)} ×${made}`);renderCraftModal();renderHotbar();renderHUD(true)}
function recipeList(){const q=S.recipeQuery.trim().toLowerCase();return RECIPES.filter(r=>r.station==='workbench'&&(!q||`${label(r.out.id)} ${r.id}`.toLowerCase().includes(q)))}
function renderCraftModal(){if(!buildingNear('workbench'))return closeModal();const list=recipeList();modalShell(`▧ ${tt('craft')}`,`<div class="craft-layout"><aside class="recipe-book"><input id="recipeSearch" class="input" placeholder="${tt('search')}" value="${esc(S.recipeQuery)}"><div class="recipe-list">${list.map(r=>`<button class="recipe-card ${recipeLocked(r)?'locked':''}" data-recipe="${r.id}"><span>${ITEM_DEFS[r.out.id]?.icon||'?'} ${esc(label(r.out.id))}</span><small>${Object.entries(r.ingredients).map(([k,v])=>`${v} ${esc(label(k))}`).join(' · ')}</small><em>${recipeLocked(r)?tt('locked'):(canRecipe(r)?'✓':'')}</em></button>`).join('')}</div></aside><section class="craft-detail"><h3>${tt('inventory')}</h3><div class="inventory-grid">${S.player.inventory.map((s,i)=>slotHtml(s,i)).join('')}</div><p class="muted">${tt('left')} · ${tt('right')} · Shift+${tt('craft')} = MAX</p></section></div>`);const search=U.gameModal.querySelector('#recipeSearch');search.oninput=e=>{S.recipeQuery=e.target.value;renderCraftModal();requestAnimationFrame(()=>{const n=U.gameModal.querySelector('#recipeSearch');n?.focus();n?.setSelectionRange(n.value.length,n.value.length)})};U.gameModal.querySelectorAll('[data-recipe]').forEach(b=>b.onclick=e=>craftRecipe(b.dataset.recipe,e.shiftKey));bindInventorySlots()}
function researchAffordable(r){return Object.entries(r.cost).every(([k,v])=>(S.bank[k]||0)>=v)}
function doResearch(id){const r=RESEARCH[id];if(!r||S.tech.has(id)||(r.requires||[]).some(x=>!S.tech.has(x)))return;if(!researchAffordable(r))return toast(tt('noMaterials'));for(const[k,v]of Object.entries(r.cost))S.bank[k]-=v;S.tech.add(id);if(id==='corePlating'){S.heart.maxHp+=350;S.heart.hp=Math.min(S.heart.maxHp,S.heart.hp+350)}if(id==='wallI'||id==='wallII')for(const b of S.buildings.filter(b=>b.type==='wall')){const ratio=b.hp/b.maxHp;b.maxHp=BUILDINGS.wall.hp*(1+(S.tech.has('wallI')?.35:0)+(S.tech.has('wallII')?.35:0));b.hp=b.maxHp*ratio}toast(tt('researchDone'));renderResearchModal();renderBuildDock();renderHUD(true)}
function researchName(id){const names={wallI:'강화벽 I',wallII:'강화벽 II',fortify:'구조 보강',corePlating:'HEART 장갑',powerGrid:'전력망',fieldRepair:'현장 수리',logistics:'물류 확장',rescue:'구조 프로토콜',survey:'자원 조사'};return i18n.lang==='ko'?(names[id]||id):id.replace(/([A-Z])/g,' $1')}
function renderResearchModal(){if(!buildingNear('researchBench'))return closeModal();modalShell(`⌬ ${tt('research')}`,`<div class="research-tree">${Object.entries(RESEARCH).map(([id,r])=>{const done=S.tech.has(id),locked=(r.requires||[]).some(x=>!S.tech.has(x)),ok=researchAffordable(r);return `<button class="research-node ${done?'done':''} ${locked?'locked':''}" data-research="${id}" ${done||locked?'disabled':''}><b>${esc(researchName(id))}</b><small>${Object.entries(r.cost).map(([k,v])=>`${v} ${esc(label(k))}`).join(' · ')}</small><span>${done?'✓':locked?tt('locked'):ok?'READY':'NEED RESOURCES'}</span></button>`}).join('')}</div>`);U.gameModal.querySelectorAll('[data-research]').forEach(b=>b.onclick=()=>doResearch(b.dataset.research))}
function renderCurrentModal(){if(S.modal==='inventory')renderInventoryModal();else if(S.modal==='craft')renderCraftModal();else if(S.modal==='research')renderResearchModal()}
function updatePlayer(dt){const p=S.player;p.cd=Math.max(0,p.cd-dt);p.dashCd=Math.max(0,p.dashCd-dt);p.dashT=Math.max(0,p.dashT-dt);if(p.dead)return;const dx=(S.keys.has('KeyD')?1:0)-(S.keys.has('KeyA')?1:0),dy=(S.keys.has('KeyS')?1:0)-(S.keys.has('KeyW')?1:0);if(dx||dy)move(p,dx,dy,p.dashT>0?PLAYER.dashSpeed:PLAYER.speed,dt);pickup(p)}
function planAI(a){a.think=.4;const e=nearest(S.enemies,a,520,x=>x.spawnDelay<=0&&x.hp>0);if(e)return a.goal={type:'fight',target:e};const n=nearest(S.nodes,a,650,x=>x.hp>0);a.goal=n?{type:'gather',target:n}:{type:'home',target:S.heart}}
function updateAI(dt){for(const a of S.ais){a.cd=Math.max(0,a.cd-dt);a.think-=dt;if(a.dead)continue;pickup(a);if(a.think<=0||!a.goal||a.goal.target?.hp<=0)planAI(a);const g=a.goal;if(!g)continue;if(g.type==='fight'){if(dist(a,g.target)<76&&a.cd<=0){g.target.hp-=18;a.cd=.58}else move(a,g.target.x-a.x,g.target.y-a.y,150,dt)}else if(g.type==='gather'){if(dist(a,g.target)<76){g.target.hp-=14*dt*3;if(g.target.hp<=0){const d=NODE_TYPES[g.target.type];drop(d.resource,d.amount,g.target.x,g.target.y);g.target.hp=0;g.target.respawn=S.time+12}}else move(a,g.target.x-a.x,g.target.y-a.y,126,dt)}else move(a,S.heart.x-a.x,S.heart.y+90-a.y,115,dt)}}
function spawnEnemy(type,delay=0){const d=ENEMIES[type],side=Math.floor(Math.random()*4),mul={easy:.86,normal:1,hard:1.2}[S.detail.room?.difficulty]||1,scale=(1+S.wave*.08)*mul;let x=side===0?5:side===1?WORLD.width-5:rand(0,WORLD.width),y=side===2?5:side===3?WORLD.height-5:rand(0,WORLD.height);S.enemies.push({type,x,y,radius:d.radius,hp:d.hp*scale,maxHp:d.hp*scale,speed:d.speed,damage:d.damage*mul,reward:d.reward,color:d.color,spawnDelay:delay,cd:0})}
function beginWave(){S.wave++;S.phase='wave';S.phaseTime=WORLD.baseWaveSeconds+S.wave*2;for(let i=0;i<Math.min(40,6+S.wave*3);i++){const r=Math.random();spawnEnemy(S.wave>2&&r<.15?'brute':r<.35?'runner':'grunt',i*.22)}if(S.wave%5===0)spawnEnemy('boss',1.5);banner(`WAVE ${S.wave}`)}
function updateEnemies(dt){for(const e of S.enemies){if(e.spawnDelay>0){e.spawnDelay-=dt;continue}if(e.hp<=0)continue;e.cd=Math.max(0,e.cd-dt);let obstacle=nearest(S.buildings,e,e.radius+36,b=>b.hp>0);if(obstacle){if(e.cd<=0){obstacle.hp-=e.damage*(S.tech.has('fortify')?.8:1);e.cd=.75}continue}const target=nearest(live(),e,90)||S.heart;if(dist(e,target)<(target.radius||24)+e.radius+5){if(e.cd<=0){target.hp=Math.max(0,target.hp-e.damage);e.cd=.72;if(target===S.heart&&target.hp<=0)finish()}}else move(e,target.x-e.x,target.y-e.y,e.speed,dt)}for(let i=S.enemies.length-1;i>=0;i--){const e=S.enemies[i];if(e.hp<=0){S.bank.crystal+=e.reward;S.score+=e.reward*10;S.enemies.splice(i,1)}}S.buildings=S.buildings.filter(b=>b.hp>0)}
function support(dt){if(S.tech.has('fieldRepair'))for(const r of S.buildings.filter(b=>b.type==='relay')){const powered=S.buildings.some(g=>g.type==='generator'&&dist(g,r)<300);if(powered)for(const b of S.buildings)if(b!==r&&dist(b,r)<190)b.hp=Math.min(b.maxHp,b.hp+10*dt)}}
function phase(dt){if(S.phase==='prep'||S.phase==='recovery'){S.phaseTime-=dt;if(S.phaseTime<=0){S.phase='warning';S.phaseTime=WORLD.warningSeconds;banner('⚠')}}else if(S.phase==='warning'){S.phaseTime-=dt;if(S.phaseTime<=0)beginWave()}else if(S.phase==='wave'){S.phaseTime=Math.max(0,S.phaseTime-dt);if(!S.enemies.length){S.phase='recovery';S.phaseTime=WORLD.recoverySeconds;S.score+=50+S.wave*10;banner('RECOVERY')}}}
function update(dt){if(!S||S.paused||S.modal||S.over)return;S.time+=dt;updatePlayer(dt);updateAI(dt);for(const n of S.nodes)if(n.hp<=0&&n.respawn&&S.time>=n.respawn)respawnNode(n);phase(dt);updateEnemies(dt);support(dt);for(const p of S.particles){p.x+=p.vx*dt;p.y+=p.vy*dt;p.life-=dt}S.particles=S.particles.filter(p=>p.life>0)}
function screenWorld(){return{x:S.mouse.x+S.camera.x,y:S.mouse.y+S.camera.y}}
function bindGlobal(){if(bound)return;bound=true;window.addEventListener('keydown',e=>{if(!S)return;if(document.activeElement===U.chatInput)return;if(e.code==='Escape'){if(S.modal)return closeModal();togglePause();e.preventDefault();return}if(S.over||S.paused)return;if(e.code==='KeyE'){openContext();e.preventDefault();return}if(/^Digit[1-9]$/.test(e.code)){selectHotbar(Number(e.code.slice(-1))-1);return}if(e.code==='Space'&&!S.player.dashCd){S.player.dashCd=PLAYER.dashCooldown;S.player.dashT=PLAYER.dashDuration;burst(S.player.x,S.player.y,'#8ddcff',14);e.preventDefault()}S.keys.add(e.code)});window.addEventListener('keyup',e=>S?.keys.delete(e.code));document.addEventListener('visibilitychange',()=>{if(document.hidden&&S&&!S.over&&!S.modal){S.paused=true;renderOverlay()}})}
function bindInstance(){canvas.onmousemove=e=>{const r=canvas.getBoundingClientRect();S.mouse.x=(e.clientX-r.left)*canvas.width/r.width;S.mouse.y=(e.clientY-r.top)*canvas.height/r.height};canvas.onmousedown=e=>{if(e.button!==0||S.modal||S.paused)return;const w=screenWorld();if(heldDef()?.toolType==='hammer'&&S.buildType)place(S.buildType,w.x,w.y);else attackOrGather()};canvas.oncontextmenu=e=>e.preventDefault();U.inventoryButton.onclick=openContext;U.pauseBtn.onclick=togglePause;U.helpBtn.onclick=()=>{S.paused=true;renderOverlay(true)};U.gameModal.oncontextmenu=e=>e.preventDefault()}
function togglePause(){if(S.over||S.modal)return;S.paused=!S.paused;renderOverlay()}
function renderOverlay(help=false){U.overlay.classList.toggle('hidden',!S.paused&&!S.over);if(!S.paused||S.over)return;U.overlay.innerHTML=`<div class="overlay-card"><h2>${help?'RE:BUILDFRONT':tt('pause')}</h2><p>${tt('toolHint')}</p><p>${tt('left')}<br>${tt('right')}<br>${tt('shift')}<br>${tt('double')}</p><div class="row"><button class="btn primary" data-resume>${tt('resume')}</button><button class="btn" data-return>${tt('return')}</button></div></div>`;U.overlay.querySelector('[data-resume]').onclick=()=>{S.paused=false;renderOverlay()};U.overlay.querySelector('[data-return]').onclick=returnLobby}
function returnLobby(){S=null;root.innerHTML='';root.classList.add('hidden');window.dispatchEvent(new Event('rbf-return-lobby'))}
function finish(){if(S.over)return;S.over=true;U.overlay.classList.remove('hidden');U.overlay.innerHTML=`<div class="overlay-card"><h2>HEART LOST</h2><p>Wave ${S.wave} · Score ${S.score}</p><button class="btn primary" data-retry>RETRY</button><button class="btn" data-return>${tt('return')}</button></div>`;U.overlay.querySelector('[data-retry]').onclick=()=>start(S.detail);U.overlay.querySelector('[data-return]').onclick=returnLobby}
function renderHUD(force=false){if(!S)return;const set=(id,v)=>{if(force||S.cache[id]!==String(v)){S.cache[id]=String(v);U[id].textContent=v}},width=(id,v)=>U[id].style.width=`${clamp(v,0,100)}%`;set('heartHp',`${Math.ceil(S.heart.hp)} / ${S.heart.maxHp}`);width('heartBar',S.heart.hp/S.heart.maxHp*100);set('waveText',S.wave);set('phaseText',S.phase==='wave'&&S.phaseTime<=0?tt('overtime'):`${S.phase} · ${Math.ceil(Math.max(0,S.phaseTime))}s`);set('zoneText',zoneAt(S.player.x,S.player.y).id);set('scoreText',S.score);set('dashText',S.player.dashCd?`${S.player.dashCd.toFixed(1)}s`:tt('ready'));width('dashBar',(1-S.player.dashCd/PLAYER.dashCooldown)*100);U.objective.innerHTML=`<span class="eyebrow">${tt('objective')}</span><b>${tt(S.phase==='prep'?'prep':S.phase==='warning'?'warning':S.phase==='wave'?'wave':'recovery')}</b><small>${tt('remaining')}: ${S.enemies.filter(e=>e.spawnDelay<=0).length}</small>`;U.resourceGrid.innerHTML=Object.entries(S.bank).map(([k,v])=>`<div class="resource-chip"><span>${RESOURCES[k].icon}</span><b>${esc(label(k))}</b><strong>${Math.floor(v)}</strong></div>`).join('');U.squadList.innerHTML=all().map(p=>`<div class="squad-row"><b>${p===S.player?'YOU':esc(p.name)}</b><div class="squad-hp"><i style="width:${clamp(p.hp/p.maxHp*100,0,100)}%"></i></div><span>${Math.ceil(p.hp)}/${p.maxHp}</span></div>`).join('');const hit=interaction();U.interactionHint.textContent=hit?`E · ${hit.type==='craft'?tt('craft'):tt('research')}`:tt('interact')}
function draw(){ctx.clearRect(0,0,canvas.width,canvas.height);S.camera.x=clamp(S.player.x-canvas.width/2,0,WORLD.width-canvas.width);S.camera.y=clamp(S.player.y-canvas.height/2,0,WORLD.height-canvas.height);ctx.save();ctx.translate(-S.camera.x,-S.camera.y);for(const z of ZONES){ctx.fillStyle=z.color;ctx.fillRect(z.x,z.y,z.w,z.h);ctx.strokeStyle=z.accent;ctx.strokeRect(z.x,z.y,z.w,z.h)}ctx.fillStyle='#72d2ff';ctx.beginPath();ctx.arc(S.heart.x,S.heart.y,S.heart.radius,0,Math.PI*2);ctx.fill();for(const n of S.nodes)if(n.hp>0){ctx.fillStyle=n.color;ctx.beginPath();ctx.arc(n.x,n.y,n.radius,0,Math.PI*2);ctx.fill()}for(const d of S.drops){ctx.fillStyle=RESOURCES[d.id]?.color||'#fff';ctx.beginPath();ctx.arc(d.x,d.y,7,0,Math.PI*2);ctx.fill();ctx.fillStyle='#fff';ctx.font='10px system-ui';ctx.fillText(RESOURCES[d.id]?.icon||'',d.x-4,d.y+3)}for(const b of S.buildings){const R=rect(b);ctx.fillStyle=b.color;ctx.fillRect(R.x+3,R.y+3,R.w-6,R.h-6);ctx.fillStyle='#fff';ctx.font='10px system-ui';ctx.fillText(b.type,b.x-18,b.y+3)}for(const p of all()){ctx.fillStyle=p.color;ctx.beginPath();ctx.arc(p.x,p.y,p.radius,0,Math.PI*2);ctx.fill();ctx.fillStyle='#fff';ctx.font='11px system-ui';ctx.fillText(p.name,p.x-p.name.length*3,p.y-p.radius-8)}for(const e of S.enemies.filter(e=>e.spawnDelay<=0)){ctx.fillStyle=e.color;ctx.beginPath();ctx.arc(e.x,e.y,e.radius,0,Math.PI*2);ctx.fill()}for(const p of S.particles){ctx.globalAlpha=clamp(p.life/.5,0,1);ctx.fillStyle=p.color;ctx.fillRect(p.x-2,p.y-2,4,4)}ctx.globalAlpha=1;if(heldDef()?.toolType==='hammer'&&S.buildType){const w=screenWorld(),f=footprint(S.buildType,w.x,w.y),R={x:f.gx*grid(),y:f.gy*grid(),w:f.gw*grid(),h:f.gh*grid()},ok=placementCheck(f);ctx.fillStyle=ok?'rgba(90,220,150,.3)':'rgba(255,80,90,.3)';ctx.strokeStyle=ok?'#75dfa3':'#ff7878';ctx.fillRect(R.x,R.y,R.w,R.h);ctx.strokeRect(R.x,R.y,R.w,R.h)}ctx.restore()}
function burst(x,y,color,n=8){if(S.detail.settings?.reducedMotion)return;for(let i=0;i<n;i++){const a=Math.random()*Math.PI*2,s=rand(30,100);S.particles.push({x,y,vx:Math.cos(a)*s,vy:Math.sin(a)*s,life:.5,color})}}
function toast(m){U.toast.textContent=m;U.toast.classList.remove('hidden');clearTimeout(toast.t);toast.t=setTimeout(()=>U.toast?.classList.add('hidden'),1500)}
function banner(m){U.banner.textContent=m;U.banner.classList.remove('hidden');clearTimeout(banner.t);banner.t=setTimeout(()=>U.banner?.classList.add('hidden'),1500)}
function loop(now){if(!S)return;const dt=Math.min(.033,Math.max(0,(now-S.last)/1000));S.last=now;update(dt);renderHUD();draw();requestAnimationFrame(loop)}
